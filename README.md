A simple copy of 9gag made with Rails.

I'm using some gems to make the upload images(carrierwave), the pagination of posts(will_paginate) and the user's authentication(devise)

To sign up your admin:

http://localhost:3000/users/sign_up

And http://localhost:3000/users/sign_in to Sign in 